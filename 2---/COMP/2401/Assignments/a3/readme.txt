Name: Braeden Hall
Student #: 101143403

Purpose: Implement doubly linked list and dynamic array data structures that can store movie data ordered by title or by year

Source Files:
- main.c (implements the specified the overall control flow for the program)
- print.c (contains functions to do with printing things to the screen for the user to see)
- init.c (contains functions to do with initializing data structures used during the program)
- data.c (contains functions to do with creating and updating the data stored in the data structures)

Header Files:
defs.h (contains the structure definitions for all custom data types used in the program. Also contains prototypes for all functions used)

Instructions:
- run the Makefile using the make command
- run the executable file that was created (a3).